<%- include('../Partials/Header') %>

    <!-- ======= Sidebar ======= -->
    <%- include('../Partials/sidebar') %>
        <main id="main" class="main">
            <div class="pagetitle">
                <h1>Product</h1>
                <nav>
                    <ol class="breadcrumb">
                        <li class="breadcrumb-item"><a href="/">Home</a></li>
                        <li class="breadcrumb-item">Tables</li>
                        <li class="breadcrumb-item active">Product</li>
                    </ol>
                </nav>
            </div>
            <section class="section">
                <div class="row">
                    <div class="col-lg-12">
                        <div class="card">
                            <div class="card-body">
                                <h5 class="card-title">Product</h5>
                                <!-- Default Table -->
                                <table class="table">
                                    <thead>
                                        <tr>
                                            <th scope="col">#</th>
                                            <th scope="col">Name</th>
                                            <th scope="col">SubCategory</th>
                                            <th scope="col">Category</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        <% extracategories.map((extracategory, index)=> { %>
                                            <tr>
                                                <th scope="row">
                                                    <%= index+1 %>
                                                </th>
                                                <td>
                                                    <%= extracategory.name %>
                                                </td>
                                                <td>
                                                    <% if (extracategory.subCategoryID) { %>
                                                        <%= extracategory.subCategoryID.name %>
                                                            <% } %>
                                                </td>
                                                <td>
                                                    <% if (extracategory.subCategoryID.categoryID) { %>
                                                        <%= extracategory.subCategoryID.categoryID.name %>
                                                            <% } %>
                                                </td>
                                            </tr>
                                            <% }) %>
                                    </tbody>
                                </table>
                                <!-- End Default Table Example -->
                            </div>
                        </div>
                    </div>
                </div>
            </section>

        </main>
        <%- include('../Partials/Footer') %>